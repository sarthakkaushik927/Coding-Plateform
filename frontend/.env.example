# Set this when the frontend and backend are deployed on different origins.
# If omitted in production, the frontend will call the same origin at /api.
VITE_API_BASE_URL=https://your-backend-domain.com/api
